{% extends "base.html.twig" %}

{% block title %}Modifier Courrier - Chrono{% endblock %}

{% block body %}

    <main class="d-flex">

        {% include "sidebar.html.twig" %}

        <section class="main">

            <section class="content">

                <h4>Modifier le courrier</h4>

                {{ form_start(form) }}
                <div>
                    {{ form_row(form.iNumero, {'label': 'Numéro'}) }}
                    {{ form_row(form.vLibelle, {'label': 'Libellé'}) }}
                    {{ form_row(form.vDestinataire, {'label': 'Destinataire'}) }}
                    {{ form_row(form.iIdUser, {'label': 'Id User'}) }}
                    <div class="d-flex align-items-center">
                        {{ form_row(form.Modifier, {'label': 'Modifier '}) }}
                        <a href="{{ path('service') }}" class="link-black ml-2">Annuler</a>
                    </div>
                </div>
                {{ form_end(form) }}

            </section>

        </section>

    </main>

{% endblock %}